# Notification Design Smoke

This temporary document exercises the Branchy Telegram notification layout.

## Commit Layout

Commit notifications should separate the branch, commit list, actor, and compare
link so the message scans cleanly in Telegram.

## Release Notes Layout

Release notifications should preserve practical GitHub Markdown like headings,
lists, links, inline code, blockquotes, and escaped text such as `<format> &`.
